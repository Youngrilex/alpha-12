// import React from 'react';
// import { SidebarDesktop, MobileSidebar } from './sidebar'; // Adjust the import based on your file structure

// const SidebarLayout = ({ darkMode }: { darkMode: boolean }) => {
//   const [darkMode, setDarkMode] = React.useState(false);
//   const [showContent, setShowContent] = React.useState(false);

//   const toggleDarkMode = () => {
//     setDarkMode((prev) => !prev);
//   };

//   const handleToggleContent = () => {
//     setShowContent((prev) => !prev);
//   };

//   return (
//     <div className={`layout ${darkMode ? 'dark' : 'light'}`}>
//       <SidebarDesktop darkMode={darkMode} toggleDarkMode={toggleDarkMode} showContent={showContent} />
//       <MobileSidebar darkMode={darkMode} handleToggleContent={handleToggleContent} />
//       {/* Other layout components */}
//     </div>
//   );
// };

// export default SidebarLayout;
